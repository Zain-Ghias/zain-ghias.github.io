---
title: "Zain Ghias"
about:
  id: main-content
  template: trestles
  image: images/IMG_8377.png
  image-shape: round
  links:
    - icon: envelope
      href: mailto:zain.ghias@mail.utoronto.ca
    - icon: github
      href: https://github.com/Zain-Ghias
    - icon: linkedin
      href: https://www.linkedin.com/in/zain-ghias/
format:
  html:
    toc: false
---

::: {.kpi-section .kpi-section-top}
::: {.kpi-section-title}
Key Metrics
:::

::: {.kpi-grid}
::: {.kpi-card}
::: {.kpi-value-wrapper}
::: {.kpi-badge}
3/4 Year
:::
::: {.kpi-value}
75%
:::
:::
::: {.kpi-label}
Applied Statistics Degree
<span class="kpi-degree-type">HBSc</span>
:::
::: {.progress-bar-container}
<div class="progress-bar" data-progress="75"></div>
:::
:::

::: {.kpi-card}
::: {.kpi-value}
4
:::
::: {.kpi-label}
Work Experience
:::
:::

::: {.kpi-card}
::: {.kpi-value}
2
:::
::: {.kpi-label}
Analytics Projects
:::
:::
:::
:::

---

::: {.about-intro-card}
## About Me

I'm Zain, a third year **Honours Bachelor of Science** student in **Applied Statistics** at the **University of Toronto**, with minors in **Computer Science** and **Mathematics**.

I'm focused on **data analytics** and **data visualization**, and I enjoy working with real datasets through projects and applied work. I like understanding how data is generated, cleaning and validating it, and making sure results are reasonable before using them to support decisions. Over time, I'm aiming to move deeper into **data science**, **automation**, and more model driven work.
:::

::: {.about-section-card}
### Current Work

I'm currently a **Data Analyst Intern** at **African Geospace**, contributing to **Wholeviz**, an AI powered data visualization project designed to make data exploration and chart creation easier for non technical users such as NGOs, journalists, and public sector teams.

My work focuses on **data quality** and **validation** across data processing workflows. I review dataset joins, edge cases, and analytical outputs to ensure pipelines behave correctly and results are accurate and understandable for users. I also work closely with analysts and engineers to document data flows and make sure backend logic aligns with what is ultimately shown in the product.

Being involved in an **AI assisted, real world product** has given me practical exposure to how data, analytics, and software come together in a production environment, especially when building tools for non technical audiences.
:::

::: {.about-section-card}
### More About Me

Alongside my internship, I'm completing the **IBM Data Analytics Professional Certificate** and continuing to build my skills in **Excel**, **SQL**, **Python with Pandas**, and **business intelligence tools**. I regularly work on analytics projects outside of coursework to practice working end to end with data, from cleaning and analysis to presentation.

My academic background in **statistics** helps me reason more carefully about data, and I'm gradually building toward more advanced **analytics** and **machine learning** topics.

Outside of work, I enjoy volleyball, table tennis, and long walks around Mississauga. I'm generally curious about how systems work, whether that's a dataset, a business process, or an analytical workflow, and I like turning that curiosity into practical projects.

**Get in touch:** [zain.ghias@mail.utoronto.ca](mailto:zain.ghias@mail.utoronto.ca)
:::

::: {.callout-tip title="Currently Open to Summer 2026 Internships"}
Looking for opportunities in **Data Analytics**, **Business Analysis**, or **Data Science**. Open to roles focused on **analytical problem-solving**, **data pipelines**, and **insight generation**.
:::


::: {.section-header}
## <span class="projects-icon">Projects</span>
<span class="section-badge">2 Active</span>
:::

::: {.projects-grid}
::: {.project-card}
::: {.project-image-container}
![](projects/bikeshare/images/Bike_Share_Toronto_logo.png){.project-image}
:::

::: {.project-content}
**Bike Share Toronto Analysis**

Analyzing ridership patterns year-over-year to assess network efficiency and demand alignment. Financial estimation analysis for trips to evaluate operational performance and resource allocation.

**Stack:** Excel · Tableau

<a href="projects/bikeshare/index.qmd" class="project-btn">View Project</a>
:::
:::

::: {.project-card}
::: {.project-image-container}
![](projects/customer-profitability/images/OverviewStatic.png){.project-image .dashboard-preview}
:::

::: {.project-content}
**Customer & Product Profitability Analytics Dashboard**

End-to-end analytics project using SQL for data cleaning and transformation, with Power BI dashboards revealing revenue trends, profitability, and customer behavior insights.

**Stack:** SQL · Power BI · Python

<a href="projects/customer-profitability/index.qmd" class="project-btn">View Project</a>
:::
:::
:::

---

::: {.section-header}
## <span class="experience-icon">Experience</span>
<span class="section-badge">2 Active</span>
:::

::: {.experience-grid}
::: {.experience-card .experience-active}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**Data Analyst Intern**  
*African Geospace*  
*Dec 2025 – Present · Remote*

QA, validation, and documentation for cloud-based data visualization platform.
:::

::: {.card-back}
**Key Responsibilities**

• Contributing to QA, validation, and documentation for Wholeviz, a cloud-based data visualization platform that enables no-code chart creation and data exploration, with a focus on user clarity and correctness.

• Reviewing and testing data processing pipelines, including dataset joins, data quality checks, and evaluation outputs, using metrics such as accuracy, precision, and recall to ensure correctness and clear user-facing behavior.

• Collaborating with data analysts and engineers to document data flows, edge cases, and backend integration notes, ensuring analytical logic and user-facing behavior are well aligned.

**Technologies**

Data Pipelines · Quality Assurance · Python · SQL · Git

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::

::: {.experience-card}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**Software Engineer Intern**  
*African Geospace*  
*Sep 2025 – Dec 2025 · 4 mos*

Developing AI-powered no-code data visualization platform at early-stage startup.
:::

::: {.card-back}
**Key Responsibilities**

• Developing an AI-powered no-code data visualization platform at an early-stage startup that supports NGOs, journalists, and public teams in creating accessible dashboards and visual stories.

• Worked on UI/UX design and front-end implementation of the Visualize page, the main interface where users create charts and receive AI chart suggestions. Contributed to building the platform's MVP by designing and implementing the workflow for creating, refining, and sharing visualizations.

• Collaborated with engineers and data analysts to enhance design consistency, improve performance, and expand visualization features using React, Next.js, Tailwind, TypeScript, and Plotly.

**Technologies**

React · Next.js · TypeScript · Tailwind · Plotly · Data Visualization · Git

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::

::: {.experience-card .experience-active}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**Frontend Developer**  
*Digital Placemaking Canada*  
*Nov 2025 – Present · Contract Part-time*

Frontend development and UX redesign for AI-driven civic-intelligence platform.
:::

::: {.card-back}
**Key Responsibilities**

• Contributing to the frontend development and UX redesign of KinesisIQ, an AI-driven civic-intelligence platform used by public-sector and community organizations. Building responsive web pages, dashboard layouts, and interactive data visualizations that support insight generation and storytelling.

• Collaborating with designers, developers, and stakeholders to refine user flows, improve usability, and align the platform's interface with project goals. Developing reusable React components and user-centric features that enhance engagement.

• This contract project is completed through the IBM SkillsBuild × Riipen program.

**Technologies**

React · HTML · CSS · JavaScript · UX Design · Responsive Web Design · Data Visualization

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::

::: {.experience-card}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**Frontend Developer Intern**  
*Quor*  
*May 2025 – Aug 2025 · 4 mos*

Built and refined cross-platform mobile app with small startup team.
:::

::: {.card-back}
**Key Responsibilities**

• Worked with a small startup team to build and refine a cross-platform mobile app. Designed and developed app screens and navigation flows using React Native (Expo) and TypeScript.

• Created reusable UI components for consistent styling and faster development. Used Android Studio virtual devices to test layouts across multiple screen sizes, ensuring responsive design.

• Built a dynamic map component with location pins and styled overlay. Added search bar, user cards, and filter options to enhance user experience. Helped keep the code organized, modular, and easy to update while building from the ground up.

• Contributed to a shared codebase on GitHub, using pull requests, code reviews, and version control to coordinate development with the team.

**Technologies**

React Native · TypeScript · JavaScript · Android Studio · Git · Mobile Development

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::
:::

---

::: {.section-header}
## <span class="cert-icon">Certifications & Professional Training</span>
<span class="section-badge">1 Active</span>
:::

::: {.experience-grid}
::: {.experience-card .certificate-card .certificate-ongoing}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**Data Analytics Professional Certificate**  
*IBM via Coursera*  
*Oct 2025 – Ongoing*

Comprehensive training in Excel, SQL, Python, and business intelligence.
:::

::: {.card-back}
**Coursework**

Excel fundamentals and advanced data analysis. SQL for data extraction and manipulation. Python programming with Pandas for data science. IBM Cognos Analytics for business intelligence and data visualization.

**Skills Gained**

Data Analysis · Statistical Methods · Business Intelligence · Data Visualization

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::

::: {.experience-card .certificate-card .certificate-completed}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**Data Analysis | SQL, Tableau, Power BI & Excel**  
*Udemy*  
*Oct 2025*

Real projects covering data cleaning, business intelligence, and data visualization.
:::

::: {.card-back}
**Coursework**

Comprehensive data analysis training with hands-on projects. SQL for data extraction and manipulation. Tableau and Power BI for business intelligence and data visualization. Excel for data cleaning and analysis.

**Skills Gained**

Data Cleaning · Business Intelligence · MySQL · SQL · Tableau · Power BI · Data Visualization · Statistics · Data Analytics

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::

::: {.experience-card .certificate-card .certificate-completed}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**The Web Developer Bootcamp 2024**  
*Udemy*  
*Aug 2024*

74-hour comprehensive full-stack web development course.
:::

::: {.card-back}
**Coursework**

Full-stack web development covering frontend and backend technologies. React.js for frontend development. Node.js, Express.js, and MongoDB for backend. HTML5, CSS3, and JavaScript fundamentals.

**Skills Gained**

React.js · Node.js · MongoDB · Express.js · JavaScript · HTML5 · CSS3 · Full-Stack Development · Responsive Web Design

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::

::: {.experience-card .certificate-card .certificate-completed}
<div class="flip-indicator"></div>
::: {.card-inner}
::: {.card-front}
**User Experience Design Fundamentals**  
*IBM SkillsBuild*  
*Nov 2025*

UX design concepts, processes, and tools for creating user-centered designs.
:::

::: {.card-back}
**Coursework**

Demonstrates knowledge of UX design concepts, processes, and tools. Understanding of user personas, wireframes, prototypes, usability testing, and collaborative UX design workflows. Experience reviewing UX design case studies and making conclusions about website redesigns.

**Skills Gained**

User Experience Design · User Interface Design · Wireframing · Prototyping · Usability Testing · User Personas · User Stories · Design Research · Information Architecture · Web Accessibility

<span class="flip-hint">← Click to flip back</span>
:::
:::
:::
:::

---

::: {.section-header}
## <span class="tools-icon">Tools & Technologies</span>
<span class="section-badge">16 Skills</span>
:::

<div class="tools-list">
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/python.svg" alt="Python" class="tool-icon"> Python</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/mysql.svg" alt="SQL" class="tool-icon"> SQL</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/pandas.svg" alt="Pandas" class="tool-icon"> Pandas</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/numpy.svg" alt="NumPy" class="tool-icon"> NumPy</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/r.svg" alt="R" class="tool-icon"> R</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/tableau.svg" alt="Tableau" class="tool-icon"> Tableau</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/powerbi.svg" alt="Power BI" class="tool-icon"> Power BI</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/microsoftexcel.svg" alt="Excel" class="tool-icon"> Excel</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/react.svg" alt="React" class="tool-icon"> React</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/nextdotjs.svg" alt="Next.js" class="tool-icon"> Next.js</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/typescript.svg" alt="TypeScript" class="tool-icon"> TypeScript</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/javascript.svg" alt="JavaScript" class="tool-icon"> JavaScript</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/tailwindcss.svg" alt="Tailwind" class="tool-icon"> Tailwind</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/html5.svg" alt="HTML" class="tool-icon"> HTML</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/css3.svg" alt="CSS" class="tool-icon"> CSS</span>
  <span class="tool-item"><img src="https://cdn.jsdelivr.net/npm/simple-icons@v11/icons/git.svg" alt="Git" class="tool-icon"> Git</span>
</div>

<script>
/**
 * Card Entrance Animation
 * 
 * Handles scroll-triggered animations for content cards:
 * 1. Cards fade in and slide up when they enter the viewport
 * 2. Border animation follows after a short delay
 * 
 * Uses IntersectionObserver for performance and smooth animations
 */
(function() {
  'use strict';
  
  const cards = document.querySelectorAll('.about-intro-card, .about-section-card, .experience-card, .project-card');
  
  // Fallback for browsers without IntersectionObserver support
  if (!('IntersectionObserver' in window)) {
    cards.forEach(function(card) {
      card.classList.add('card-visible', 'border-animated');
    });
    return;
  }
  
  // Observer for card entrance animation
  const cardObserver = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        const card = entry.target;
        
        // Step 1: Make card visible (fade in + slide up)
        card.classList.add('card-visible');
        
        // Step 2: After card appears, trigger border animation
        setTimeout(function() {
          card.classList.add('border-animated');
        }, 300); // 300ms delay after card appears
        
        cardObserver.unobserve(card);
      }
    });
  }, {
    threshold: 0.2, // Trigger when 20% of card is visible
    rootMargin: '0px 0px -50px 0px' // Start animation slightly before card fully enters
  });
  
  // Observe all cards
  cards.forEach(function(card) {
    cardObserver.observe(card);
  });
})();

/**
 * Progress Bar Animation
 * 
 * Animates the degree progress bar when it enters the viewport.
 * Delayed to start after the first KPI card animation completes for better visual flow.
 */
(function() {
  'use strict';
  
  // Fallback for browsers without IntersectionObserver support
  if (!('IntersectionObserver' in window)) {
    document.querySelectorAll('.progress-bar').forEach(function(bar) {
      const progress = bar.getAttribute('data-progress');
      if (progress) {
        setTimeout(function() {
          bar.style.width = progress + '%';
        }, 100);
      }
    });
    return;
  }
  
  const progressObserver = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        const bar = entry.target;
        const progress = bar.getAttribute('data-progress');
        
        if (progress && !bar.classList.contains('animated')) {
          bar.classList.add('animated');
          // Delay progress bar to start after first card entrance animation completes
          // First card delay (300ms) + card animation (600ms) = 900ms, then add small delay
          setTimeout(function() {
            bar.style.width = progress + '%';
          }, 1000); // Start after first card animation completes with small delay
        }
        
        progressObserver.unobserve(bar);
      }
    });
  }, {
    threshold: 0.3,
    rootMargin: '0px 0px -50px 0px'
  });
  
  document.querySelectorAll('.progress-bar').forEach(function(bar) {
    progressObserver.observe(bar);
  });
  
  /**
   * KPI Card Entrance Animation
   * 
   * Handles staggered entrance animations for KPI cards:
   * 1. Cards fade in and slide up with staggered delays (300ms base + 200ms per card)
   * 2. Shimmer effect sweeps across each card after the fade-in completes
   * 3. Hover effects are enabled after all animations complete
   */
  const kpiObserver = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        const card = entry.target;
        const index = Array.from(document.querySelectorAll('.kpi-card')).indexOf(card);
        
        // Add initial state class (invisible, slightly below)
        card.classList.add('kpi-initial');
        
        // Trigger entrance animation with staggered delay
        // Base delay (300ms) + staggered delay per card (200ms)
        setTimeout(function() {
          card.classList.remove('kpi-initial');
          card.classList.add('kpi-animating');
          
          // Remove animating class after all animations complete to allow hover
          // Card animation (0.6s) + shimmer delay (1.1s) + shimmer duration (0.8s) = 2.5s total
          setTimeout(function() {
            card.classList.remove('kpi-animating');
          }, 2500); // Wait for shimmer to complete
        }, 300 + (index * 200)); // Base 300ms delay + 200ms stagger between each card
        
        kpiObserver.unobserve(card);
      }
    });
  }, {
    threshold: 0.2,
    rootMargin: '0px 0px -30px 0px'
  });
  
  // Initialize all KPI cards in initial state
  document.querySelectorAll('.kpi-card').forEach(function(card) {
    card.classList.add('kpi-initial');
    kpiObserver.observe(card);
  });
  
  /**
   * Tools Section Fade-in Animation
   * 
   * Animates the tools list when it enters the viewport
   */
  const toolsObserver = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        const toolsList = entry.target;
        // Add visible class with a small delay for smooth entrance
        setTimeout(function() {
          toolsList.classList.add('tools-visible');
        }, 200);
        toolsObserver.unobserve(toolsList);
      }
    });
  }, {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  });
  
  // Observe tools list if it exists
  const toolsList = document.querySelector('.tools-list');
  if (toolsList) {
    toolsObserver.observe(toolsList);
  }
})();

/**
 * Experience Card Flip Functionality
 * 
 * Enables click/tap to flip experience cards, revealing additional details on the back.
 * Handles both mouse clicks and touch events for mobile devices.
 * Prevents accidental flips from swipes or long presses.
 */
(function() {
  'use strict';
  
  // Wait for DOM to be ready
  function initFlipCards() {
    const experienceCards = document.querySelectorAll('.experience-card');
    
    if (experienceCards.length === 0) {
      // Retry if cards aren't loaded yet
      setTimeout(initFlipCards, 100);
      return;
    }
    
    experienceCards.forEach(function(card) {
      // Handle click/touch events
      function handleFlip(e) {
        // Prevent event bubbling
        e.stopPropagation();
        
        // Only allow flip after card is visible
        if (card.classList.contains('card-visible')) {
          card.classList.toggle('flipped');
          // Mark card as interacted to hide indicator
          card.classList.add('card-interacted');
        }
      }
      
      // Click event for desktop
      card.addEventListener('click', handleFlip);
      
      // Touch event for mobile (prevent double-tap zoom)
      let touchStartTime = 0;
      let touchStartX = 0;
      let touchStartY = 0;
      
      card.addEventListener('touchstart', function(e) {
        touchStartTime = Date.now();
        touchStartX = e.touches[0].clientX;
        touchStartY = e.touches[0].clientY;
      });
      
      card.addEventListener('touchend', function(e) {
        const touchDuration = Date.now() - touchStartTime;
        const touchEndX = e.changedTouches[0].clientX;
        const touchEndY = e.changedTouches[0].clientY;
        const deltaX = Math.abs(touchEndX - touchStartX);
        const deltaY = Math.abs(touchEndY - touchStartY);
        
        // Only flip on quick tap (not long press or swipe)
        if (touchDuration < 300 && deltaX < 10 && deltaY < 10) {
          e.preventDefault();
          handleFlip(e);
        }
      });
    });
  }
  
  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initFlipCards);
  } else {
    initFlipCards();
  }
})();

/**
 * Profile Headline Injection
 * 
 * Dynamically adds a professional headline/subtitle under the profile name.
 * Styled similar to LinkedIn profile headlines for a professional appearance.
 */
(function() {
  'use strict';
  
  function addProfileHeadline() {
    const titleBlock = document.querySelector('.quarto-title-block');
    const titleMeta = document.querySelector('.quarto-title-meta');
    
    if (!titleBlock || !titleMeta) {
      setTimeout(addProfileHeadline, 100);
      return;
    }
    
    // Check if headline already exists
    if (titleMeta.querySelector('.about-headline')) {
      return;
    }
    
    // Create headline element with two separate lines
    const headline = document.createElement('div');
    headline.className = 'about-headline';
    headline.innerHTML = '<span class="headline-line">Data Analyst Intern • African Geospace<span class="headline-icon work-icon"></span></span><br><span class="headline-line">HBSc Applied Statistics • UofT<span class="headline-icon education-icon"></span></span>';
    
    // Insert after title, before links
    titleMeta.appendChild(headline);
  }
  
  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', addProfileHeadline);
  } else {
    addProfileHeadline();
  }
})();

/**
 * Profile Image Lazy Loading
 * 
 * Implements lazy loading and fade-in animation for the profile image.
 * Improves initial page load performance and provides smooth visual feedback.
 */
(function() {
  'use strict';
  
  function initProfileImage() {
    const profileImage = document.querySelector('.about-image');
    
    if (!profileImage) {
      // Retry if image isn't loaded yet
      setTimeout(initProfileImage, 100);
      return;
    }
    
    // Add lazy loading attribute if not already present
    if (!profileImage.hasAttribute('loading')) {
      profileImage.setAttribute('loading', 'lazy');
    }
    
    // Mark as loaded once image finishes loading
    if (profileImage.complete) {
      profileImage.classList.add('loaded');
    } else {
      profileImage.addEventListener('load', function() {
        profileImage.classList.add('loaded');
      });
      
      // Fallback: mark as loaded after a timeout
      setTimeout(function() {
        profileImage.classList.add('loaded');
      }, 2000);
    }
  }
  
  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initProfileImage);
  } else {
    initProfileImage();
  }
})();
</script>